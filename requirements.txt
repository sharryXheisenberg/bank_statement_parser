# Core API
fastapi>=0.124.1,<1.0
uvicorn[standard]>=0.34,<1.0
starlette>=0.49.1,<1.0
python-dotenv==1.0.1

# PDF parsing + data handling
pdfplumber==0.11.4
pandas==2.2.2
openpyxl==3.1.5

# File upload support (pinned exact -- gradio also pins this exact version,
# so this avoids a conflict rather than causing one)
python-multipart==0.0.12

# Only needed if your environment also has other tools (datasets, google-adk)
# that require a newer requests than what's already installed
requests>=2.32.4,<3.0